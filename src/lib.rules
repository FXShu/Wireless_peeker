ifndef CC
CC=gcc
endif

ifndef CFLAGs
CFLAGS = -MMD -O2 -Wall -g
endif

CFLAGS += -I.. -I../utils

Q=@
E=echo
ifeq ($(V), 1)
Q=
E=true
endif
ifeq ($(OUIET), 1)
Q=@
E=true
endif

%.o: %.c
	$(Q)$(CC) -c -o $@ $(CFLAGS) $<
	@$(E) "  CC " $<
